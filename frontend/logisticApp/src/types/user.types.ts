export type UserProfile = {
  id: string;
  username: string;
  email: string;
  createdAt: string;
};

// TODO: по возможности подумать над этим
// export interface UserAuthInfo {
//   token: string;
//   user: UserProfile;
// }

// export type UserRole = 'admin' | 'user';
